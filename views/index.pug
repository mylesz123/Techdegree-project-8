extends layout.pug
block content

   h1 Library
   h3 Please feel free to donate to our collection.
   p#header
    a.button(href='/books/new') Add New Book
   table
      thead
        tr
          th Title
          th Author
          th Genre
          th Year
      tbody
         each book in books
            tr
               td
                  a(href='/books/' + book.id)= book.title
               td= book.author
               td= book.genre
               td= book.year
